import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
    root: {
        flexWrap: 'wrap',
        justifyContent: 'center',
        borderRadius: '0',
    },
}))
