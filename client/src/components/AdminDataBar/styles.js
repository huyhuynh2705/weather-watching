import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
    root: {
        marginTop: '20px',
    },
    table: {
        minWidth: 650,
        marginTop: '20px',
        marginBottom: '20px',
      },
}))
